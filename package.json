{
  "name": "ivy-portal",
  "version": "1.0.0",
  "description": "Personal OpenClaw dashboard — localhost god mode",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "lint": "for f in server.js public/app.js lib/telegram-indexer.js lib/whatsapp-indexer.js scripts/smoke.mjs; do node --check \"$f\" || exit 1; done",
    "smoke": "node scripts/smoke.mjs",
    "verify": "npm run lint && npm run smoke"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "express": "^4.21.0",
    "input": "^1.0.1",
    "telegram": "^2.26.22"
  }
}
